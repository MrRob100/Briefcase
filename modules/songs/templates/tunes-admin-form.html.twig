{{ attach_library('songs/songs_style') }}
{{ form }}
<div class="playlist">
    <ul>
    {% for song in songs %}
        <div>
            <li>{{ song.name }}
            <a href="{{ path('songs.delete', {'id': song.id}) }}">Delete</a>             
            </li>   
        </div>
    {% endfor %}
    </ul>
</div>
